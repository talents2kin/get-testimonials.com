import { twx } from "@/lib/twx";

export const Layout = twx.div((props)=>[
  `max-w-5xl w-full flex-col py-4 flex  gap-4 mx-auto px-4 py-6`
  
])
export const LayoutTitle = twx.h1((props)=>[
  `text-3xl font-bold`
])